services:
  backend-admin:
    build: .
    container_name: backend-admin
    ports:
      - "13286:13286"
    volumes:
      - ./config:/app/config
      - ./uploads:/app/uploads
    environment:
      - PYTHONPATH=/app
    restart: unless-stopped
    command: python main.py 